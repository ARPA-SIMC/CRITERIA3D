Minor
 - introduce and test "omp simd" and "omp atomic update" directives (if !MSVC)

 - Combine temp "_m" - methods with the free functions + #ifdef structure and/or CRTP
 - Move switch/if block in solveLinearSystem to a constexpr map (pType, method <-> std::function)

 - MAYBE: move to new[] allocation
 - MAYBE: use #pragma once instead of #ifndef / #define
 - MAYBE: remove "using namespace" directive

Major
 - Discuss branch structure
 	- Rename SF3D to SF3Dv1 and SF3Dnew to v2 (inline namespace?) or remove old version?
	- Finalize variable names
	- Complete debug checks -> Remove debug checks

Extra
 - Refactor loops outside of soilFluxes3D
 - Implement openMP parallelization

_______________________________________________________________________________________________________________________________________

Questions:
 - can heat run without water? -> if so: - move invariantFluxes pointer out of waterData struct
                                         - ...

 - why some heat loops run from index 1 (instead from index 0)?	-> if (not) an error fix loops in new/old version

